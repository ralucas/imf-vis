React = require('react');
$ = require('jquery');
charts = require('../scripts/charts');

var Map = React.createClass({

  render: function() {
    return (
      <div>Map</div>
    );
  }
});

module.exports = Map;
